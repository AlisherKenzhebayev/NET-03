using System;
using System.Collections.Generic;

namespace Source
{
    public static class BinarySearchHelper
    {
        // Should return an Enumerator to the generic collection of T
        public static IEnumerator<T> BinarySearch<T>(List<T> collection, T search, IEnumerator<T> left, IEnumerator<T> right)
        {
            IEnumerator<T> middle = right + left;
            if(collection[])
            
            return collection.GetEnumerator();
        }
    }
}